# LiveNote UI Design Rules

本文件是 LiveNote 的 UI 设计规范，适用于手机 PWA、电脑管理台和后续新增界面。

## 规范优先级

- `AGENTS.md` 规定开发约束和自动化边界。
- `PRODUCT.md` 规定产品定位、用户和业务原则。
- 本文件规定界面结构、交互、文案和视觉表达。
- 具体功能需求可以补充本规范，但不能无理由违反产品原则；如确需例外，必须在实现说明中记录原因、范围和回退方式。

## 1. 从用户结果开始

- 先描述用户要完成的结果，再决定页面结构和内部状态。
- 不把数据库表、任务状态机、Worker、文件目录或 API 作为用户的导航结构。
- 页面标题、任务行和按钮都使用用户能理解的结果语言。

## 2. 默认最短路径

- 系统能可靠完成的领取、上传、重试、状态流转、保存和恢复，都由代码自动完成。
- 用户只处理需要判断、授权或确认的动作，例如审核、纠错、发布和删除。
- 正常流程不要求用户复制提示词、搬运文件、编辑 JSON、选择结果文件或操作命令行。
- 兼容性或诊断操作必须与主流程分离，并明确标注为例外路径。

## 3. 每个状态都要可理解、可行动

每个状态必须让用户立即知道：

1. 发生了什么；
2. 是否需要用户操作；
3. 下一步是什么；
4. 操作后会得到什么结果。

状态文案优先使用结果语言，例如“已识别，待总结”“正在生成总结”“待发布”“已发布”，禁止直接展示 `TRANSCRIBED`、`PROCESSING` 等内部状态。

等待状态不能暗示系统正在自动做实际上没有开始的工作。若需要外部服务、人工判断或用户发起，必须明确写出责任方和下一步。

## 4. 一个状态一个主动作

- 每个任务状态最多有一个视觉上最主要的操作。
- 操作按钮使用“动作 + 对象 + 结果”的表达，例如“发布到手机”“重试上传”。
- 不使用“确定”“提交”“处理”等无法说明结果的按钮文字。
- 低频操作放入设置、详情或渐进式展开区域，不与主动作竞争。

## 5. 不依赖记忆，不让用户猜

- 任务行旁边直接放对应操作，不让用户在页面中寻找对象和按钮的关系。
- 会话、用户、时间、时长和任务之间必须有稳定、可读的对应关系。
- 需要跨应用或外部服务时，界面提供明确引导；每条会话必须使用唯一标识，禁止让用户凭标题猜测。
- 成功、失败和下一步反馈应出现在操作附近；不只在页面顶部显示一次。

## 6. 信息只出现一次

- 同一用户、会话、状态、发布按钮和说明只保留一个最合适的展示位置。
- 删除重复标题、重复状态、重复用户信息、重复帮助文字和无效装饰。
- 预览只展示真正需要审核的内容；原始 JSON、内部日志和调试信息默认隐藏。

## 7. 内容优先，减少视觉噪声

- 优先使用清晰的列表、分组和分隔线，不使用多层嵌套卡片组织所有内容。
- 避免无意义的大面积留白、重复边框、过重阴影和装饰性盒子。
- 高频信息同行排列、右对齐操作，减少任务行高度。
- 视觉层级通过字号、字重、间距和颜色建立，不依赖复杂装饰。

## 8. 手机优先与响应式

- 手机是核心使用场景；所有主流程必须适配窄屏和触控。
- 禁止横向滚动、过小点击目标和被底部裁切的主要内容。
- 电脑端可以展示更多管理信息，但不能让桌面宽度变成浪费空间的理由。
- 页面内容未超过视口时，不人为制造滚动区域。

## 9. 错误、等待和恢复

错误消息必须说明：失败了什么、可能原因是什么、用户如何恢复。

- 不把错误码或内部异常作为主文案。
- 可恢复错误自动重试，并在确实需要用户操作时提供一个明确动作。
- 网络中断、刷新、服务重启不能丢失已完成录音、逐字稿、总结或历史记录。
- 删除、覆盖和发布等不可逆动作必须说明对象和后果。

## 10. 术语与文案

- 界面默认使用简洁、明确的中文。
- 同一个概念全局使用同一个词，不在“识别/转写”“总结/分析”“发布/回传”之间随意切换。
- 文案短，但不能省略用户下一步所需的信息。
- 不把技术名词、缩写、英文状态或内部编号作为用户必须理解的前提。

## 11. 可访问性

- 关键状态不能只通过颜色表达，必须同时有文字。
- 所有按钮和表单控件有清晰的可访问名称。
- 保留键盘焦点、原生展开收起语义和足够的触控区域。
- 文字、占位符、焦点和错误提示在真实内容下保持可读。

## 12. 交付前检查

每次 UI 修改至少验证：

- 新用户能否在几秒内知道下一步；
- 是否存在重复信息或无效步骤；
- 主动作是否唯一且靠近对应对象；
- 状态文案是否准确描述真实系统行为；
- 手机窄屏和电脑常用宽度是否可读；
- 加载、成功、失败、空状态和恢复路径是否完整；
- 构建、键盘焦点和界面检测是否通过。

## 当前过渡说明

当前总结仍通过 Codex 聊天完成，界面中的“复制总结指令”只是临时兼容路径，不是目标体验。后续接通 Codex 调用后，应改为用户点击一次即可发起当前会话总结，并由系统自动绑定任务，不再要求用户复制、粘贴或辨认提示词。

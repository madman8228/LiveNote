# Frontend build configuration.
# Keep /api/v1 when the frontend and API share one HTTPS domain.
VITE_API_BASE_URL=/api/v1
VITE_API_TIMEOUT_MS=30000

# Optional. Set the same value as LIVENOTE_API_KEY on the API server.
VITE_API_KEY=

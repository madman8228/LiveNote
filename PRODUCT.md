# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Vue 3 + Vite + TypeScript；手机端是面向 Android Chrome 的 PWA，服务端使用 FastAPI + SQLite。

## Users

核心用户是使用手机进行直播、会议或课程录音的人。他们希望录音稳定完成，并在之后直接获得可阅读、可复习的知识总结。

次要用户是电脑端管理员。管理员负责处理异常、查看任务、审核总结和发布结果，但不应承担技术性的文件搬运、JSON 编辑或重复配置工作。

## Product Purpose

LiveNote 让用户在手机上完成录音，录音自动保存并上传，系统自动生成待处理任务和知识总结，最后把审核通过的知识卡返回手机。

产品成功的标准是：手机用户只需要完成录音和查看结果；系统自动完成上传、任务流转、处理、状态恢复和结果同步；管理员只在确实需要人工判断时介入。

## Positioning

LiveNote 是一个手机优先、本地优先的连续录音与知识卡系统：手机负责可靠采集，电脑端和服务器负责自动处理，用户最终在手机上消费结果。录音、上传、处理和发布相互独立，网络或服务短暂异常不应破坏已经完成的录音。

## Operating Context

- 手机用户在 Android Chrome PWA 中开始、停止并保存录音。
- 录音通过 IndexedDB 和上传队列保存，网络恢复后自动补传。
- 服务器生成处理任务，电脑端负责自动领取和处理任务。
- 管理员在 PC 管理页面查看任务、处理异常、审核总结并发布到手机。
- 手机端通过绑定的用户和设备身份查询自己的已发布结果。
- 电脑端管理页面是内部工具，应服务于手机端主流程，而不是把技术流程转嫁给管理员。
- 典型内容来自中国民间健康类直播：民间大夫回答普通人的问题，来电者也可能分享自己的偏方或处理经验。
- 一场内容可能持续约 120 分钟，并包含多位观众连麦；总结必须保留每位来电者的问题、回答和经验归属，不能把整场内容压成一段笼统结论。

## Capabilities and Constraints

- 手机端是核心入口和核心体验，必须优先保证窄屏可用、无横向滚动、录音不中断和状态清晰。
- 录音、上传、处理和结果查询需要支持断网恢复、刷新恢复、后台或页面生命周期变化，以及失败后的自动恢复。
- 上传期间，服务器可按连续 Chunk 窗口提前识别并显示真实的“已识别至”水位；这只是临时进度，录音结束后的完整逐字稿才是总结和发布依据。
- 系统应自动完成任务领取、音频处理、结果回传和状态轮询；不要要求用户手动下载音频、复制提示词、编辑 JSON、选择结果文件或操作命令行。
- 电脑端管理员只需要处理不可自动判断的内容，例如审核总结、确认发布和处理真正的异常。
- 用户身份通过管理员创建用户和一次性配对码绑定手机；未绑定身份时，不应让用户进入会产生无归属录音的主流程。
- 管理台需要展示任务所属用户、会话信息和当前状态，避免让管理员通过 Session ID 猜测归属。
- 电脑端 UI 当前仍在建设中，后续应优先降低操作步骤、隐藏实现细节并改善任务状态和审核流程。
- 正式识别使用本机缓存的 Whisper/ASR，转写在电脑端后台自动完成；总结由当前 Codex 聊天读取已保存文字后生成。产品不调用 OpenAI API，也不要求用户复制提示词、搬运音频或编辑 JSON。
- 健康内容总结必须区分大夫意见、来电者自述和未经验证的偏方；不得把个人经验写成已证实的疗效、诊断或治疗方案，也不得凭空补充剂量、禁忌和医学结论。
- 总结面向普通老人阅读，使用通俗、平静、接近日常说话的中文；用户可见内容不提及录音、音频、转写、识别、模型、AI、Codex 或系统处理过程。

## Brand Commitments

- 产品名称：LiveNote。
- 产品语言以简洁、明确的中文为主。
- 面向手机用户的界面应让用户感到“录音完成后系统会继续处理”，而不是要求用户理解后台任务、文件夹或数据格式。

## Evidence on Hand

- 现有项目 README、Vue 3 前端、FastAPI 服务端、IndexedDB 录音存储和任务处理接口。
- 已有手机录音、上传、用户绑定、电脑任务处理、知识卡阅读和本地草稿能力。
- 当前没有应被当作产品承诺的外部客户、商业数据、性能指标或生产部署证据。

## Product Principles

1. 手机优先：核心用户的主要任务必须在手机端简单、稳定、连续地完成。
2. 自动化优先：代码能够可靠完成的工作，系统自动完成，不交给用户重复操作。
3. 人工只做判断：人工只介入审核、确认、纠错和真正无法自动判断的异常。
4. 隐藏实现细节：用户不需要理解任务目录、JSON、提示词、Worker 或内部状态机才能完成目标。
5. 可恢复且可解释：失败、等待和恢复都要有清晰状态，系统应主动恢复可恢复的问题，并告诉用户下一步真正需要做什么。

## Accessibility & Inclusion

- 手机端必须适配窄屏和触控操作，避免横向滚动和过小的点击目标。
- 录音中的关键状态、上传状态和处理结果不能只依赖颜色表达。
- 电脑端管理页面需要支持键盘焦点、原生展开收起语义和清晰的错误反馈。
